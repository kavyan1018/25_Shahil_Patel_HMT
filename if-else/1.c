#include <stdio.h>

void main()
{
    int a, b;

    printf("Enter the First Number :");
    scanf("%d", &a);

    printf("Enter the Second Number :");
    scanf("%d", &b);

    if (a > b)
    {
        printf("A is Max !");
    }
    else{
        printf("B is Max !");
    }
    

}